{{#paper-sidenav}}
	{{#paper-drawer}}
		<h1 class="logo">{{#link-to "index"}}<i class="icon ic-chat"></i> <strong>Chinwag</strong>{{/link-to}}</h1>
		<nav class="sidenav-nav">
			<ul>
				<li>{{link-to "Settings" "settings"}}</li>
			</ul>
		</nav>
	{{/paper-drawer}}
	{{#paper-content  classNames="sidenav-static-content"}}
		{{outlet}}
	{{/paper-content}}
{{/paper-sidenav}}